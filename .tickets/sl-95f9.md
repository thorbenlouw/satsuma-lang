---
id: sl-95f9
status: closed
deps: [sl-3tv5]
links: []
created: 2026-04-02T09:19:36Z
type: task
priority: 2
assignee: Thorben Louw
parent: sl-m2s6
---
# core: remove structural/mixed classifications from classify.ts

Update classify.ts in satsuma-core so it returns only: none (no transform body), nl (any non-empty transform body), nl-derived (synthetic arrow from @ref). Remove the structural and mixed return paths. Update the Classification type. Update decomposePipeSteps() in extract.ts — all steps are NL text, no step-type distinction needed. Simplify isInlinePipeChain() in format.ts — all chains are NL so any heuristic based on NL presence is moot.

## Acceptance Criteria

1. Classification type: 'none' | 'nl' | 'nl-derived' only (structural and mixed removed)
2. classify.ts: no code path returns structural or mixed
3. decomposePipeSteps(): all steps returned as NL text, step-type field removed or collapsed
4. format.ts: isInlinePipeChain() simplified — no NL-presence heuristic needed
5. All satsuma-core unit tests updated and passing
6. No behavioural regressions in the extract/format paths for existing valid fixtures


## Notes

**2026-04-02T09:52:42Z**

**2026-04-02T10:30:00Z**

Cause: classifyTransform() analyzed pipe step node types to distinguish structural/mixed/nl, adding complexity with no functional benefit since all steps are LLM-interpreted.
Fix: collapsed Classification to nl|none|nl-derived; rewrote classifyTransform() as a simple presence check; simplified isInlinePipeChain() to only exclude multiline_string; updated all classify tests and the format test for arithmetic pipe chains.
