{
  "name": "tree-sitter-stm",
  "version": "0.0.0",
  "private": true,
  "description": "Tree-sitter grammar for the STM language",
  "license": "UNLICENSED",
  "scripts": {
    "generate": "tree-sitter generate",
    "test": "tree-sitter test && bash scripts/test-fixtures.sh"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.25.10"
  }
}
