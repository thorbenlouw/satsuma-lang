{
  "name": "tree-sitter-stm",
  "version": "2.0.0",
  "private": true,
  "description": "Tree-sitter grammar for STM v2",
  "license": "UNLICENSED",
  "main": "bindings/node",
  "scripts": {
    "generate": "tree-sitter generate",
    "build": "tree-sitter generate && node-gyp build",
    "test": "tree-sitter test",
    "smoke": "node scripts/smoke-test.js"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.25.10"
  },
  "peerDependencies": {
    "tree-sitter": "^0.21.0"
  }
}
