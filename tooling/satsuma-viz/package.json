{
  "name": "@satsuma/viz",
  "version": "0.1.0",
  "private": true,
  "description": "Satsuma mapping visualization web component",
  "type": "module",
  "main": "dist/satsuma-viz.js",
  "types": "src/satsuma-viz.ts",
  "scripts": {
    "build": "esbuild src/satsuma-viz.ts --bundle --format=esm --target=es2022 --outfile=dist/satsuma-viz.js --sourcemap --loader:.css=text --minify",
    "watch": "npm run build -- --watch",
    "test": "node --test test/**/*.test.js",
    "test:typecheck": "tsc --noEmit",
    "test:coverage": "c8 --reporter=text --reporter=lcov --reporter=json-summary --include='dist/**/*.js' --exclude='dist/**/*.d.ts' node --test test/**/*.test.js"
  },
  "dependencies": {
    "@satsuma/core": "*",
    "@satsuma/viz-model": "*",
    "elkjs": "^0.12.0",
    "lit": "^3.3.3"
  },
  "devDependencies": {
    "@lit-labs/ssr-dom-shim": "^1.6.0",
    "@satsuma/scenario-gen": "*",
    "@satsuma/viz-backend": "*",
    "@types/node": "^26.4.0",
    "c8": "^12.0.0",
    "esbuild": "^0.28.1",
    "fast-check": "^4.9.0",
    "tree-sitter-satsuma": "*",
    "typescript": "^6.0.3"
  }
}
