// Degradation: missing closing brace
// Expected: the block extends to EOF. Subsequent declarations may be
// absorbed but the highlighter should not crash or produce infinite loops.

schema unclosed {
  id INT (pk)
  name STRING
  // Missing closing }

schema next_schema {
  // This may or may not be highlighted correctly, but the file should not
  // cause catastrophic behaviour.
  x INT
}
