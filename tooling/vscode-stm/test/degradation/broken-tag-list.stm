// Degradation: unclosed tag list
// Expected: tokeniser stays in tag-list context until end of line or block,
// then recovers. Subsequent fields should still highlight normally.

source test {
  id INT [pk, required
  // ^ tag list never closed — tokeniser should recover by next line or }
  name STRING [required]
  email VARCHAR [pii]
}
