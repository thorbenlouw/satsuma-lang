================================================================================
Adjacent NL strings in note block
================================================================================

schema checkout_abandonment_rate (
  metric,
  source {fact_sessions, dim_customer},
  grain daily
) {
  note {
    "Sessions that reached checkout but did not result in a placed order, "
    "divided by all sessions that reached checkout."
  }
}

--------------------------------------------------------------------------------

(source_file
  (schema_block
    (block_label
      (identifier))
    (metadata_block
      (tag_token
        (identifier))
      (tag_with_value
        (identifier)
        (value_text
          (identifier)
          (identifier)))
      (tag_with_value
        (identifier)
        (value_text
          (identifier))))
    (schema_body
      (note_block
        (nl_string)
        (nl_string)))))

================================================================================
Single NL string in note block
================================================================================

schema customers {
  note {
    "Primary customer store."
  }
}

--------------------------------------------------------------------------------

(source_file
  (schema_block
    (block_label
      (identifier))
    (schema_body
      (note_block
        (nl_string)))))

