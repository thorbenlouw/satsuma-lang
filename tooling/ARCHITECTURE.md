# Satsuma Tooling Architecture

The canonical tooling architecture reference now lives at [`docs/developer/ARCHITECTURE.md`](../docs/developer/ARCHITECTURE.md).

This package-local file is kept only as a pointer so older links to `tooling/ARCHITECTURE.md` do not become a second source of truth.
